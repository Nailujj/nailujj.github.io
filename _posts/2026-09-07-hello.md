---
layout: post
title: "Hello"
math: true
---

Placeholder post so the writing page has something to list. Delete me.

Math works with the `math: true` front-matter flag: $E(3)$-equivariance means $f(g\cdot x) = g\cdot f(x)$ for all $g \in E(3)$.

```python
def hello():
    print("code blocks work too")
```
