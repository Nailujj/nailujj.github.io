source "https://rubygems.org"

# Jekyll 4, built by .github/workflows/jekyll.yml (GitHub's default builder is Jekyll 3).
gem "jekyll", "~> 4.3"
gem "webrick"   # needed for `jekyll serve` on Ruby >= 3.0

group :jekyll_plugins do
  gem "jekyll-seo-tag"
  gem "jekyll-feed"
end
